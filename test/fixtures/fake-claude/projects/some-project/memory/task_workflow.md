# Task Workflow Memory

Tasks are tracked using the secretary approach:
- High priority tasks get immediate attention
- Normal tasks are queued and processed daily
- Low priority tasks are batched weekly

This file is loaded only when recalled, not at startup.
